import React from "react";
import "./title.css"; // Optional: Create a separate CSS file for the title if needed

const Title = () => {
  return (
    <h1 className="portfolio-title">
     
    </h1>
  );
};

export default Title;